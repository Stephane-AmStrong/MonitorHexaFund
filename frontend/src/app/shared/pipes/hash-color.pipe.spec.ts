import { HashColorPipe } from './hash-color.pipe';

describe('HashColorPipe', () => {
  it('create an instance', () => {
    const pipe = new HashColorPipe();
    expect(pipe).toBeTruthy();
  });
});
